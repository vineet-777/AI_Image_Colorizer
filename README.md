# AI Image Colorizer

A Flask-based web application that automatically restores colors in black and white images using a pre-trained Convolutional Neural Network (CNN).

**More information can be found in the `README.md` inside the `project/` folder.**
